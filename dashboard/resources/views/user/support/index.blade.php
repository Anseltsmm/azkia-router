@extends('layouts.app')
@section('content')
<div class="top"><div><h2>{{ __('dashboard.pages.support.heading') }}</h2><p>{{ __('dashboard.pages.support.subtitle') }}</p></div><a class="btn" href="{{ route('support.create') }}">{{ __('dashboard.pages.support.create_ticket') }}</a></div>@include('admin._alerts')
<div class="table-wrap"><table><thead><tr><th>{{ __('dashboard.pages.support.number') }}</th><th>{{ __('dashboard.pages.support.subject') }}</th><th>{{ __('dashboard.pages.support.category') }}</th><th>{{ __('dashboard.common.status') }}</th><th>{{ __('dashboard.pages.support.last') }}</th></tr></thead><tbody>@forelse($tickets as $ticket)<tr><td><a href="{{ route('support.show', $ticket) }}"><strong>{{ $ticket->ticket_number }}</strong></a></td><td>{{ $ticket->subject }}</td><td>{{ __("dashboard.pages.support.cat_".strtolower($ticket->category)) }}</td><td><span class="badge">{{ __('dashboard.pages.support.st_'.strtolower($ticket->status)) }}</span></td><td>{{ $ticket->last_message_at ?: $ticket->created_at }}</td></tr>@empty<tr><td colspan="5" class="muted">{{ __('dashboard.pages.support.empty') }}</td></tr>@endforelse</tbody></table></div>{{ $tickets->links() }}
@endsection
